- name: install nginx
  hosts: localhost
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.yum:
        name: nginx
        state: latest

    - name: start nginx
      ansible.builtin.yum:
        name: nginx
        state: started
        enable: yes